/*
ให้นักศึกษาเขียนโปรแกรม รับ input เป็นความยาวฐาน และความสูงของสามเหลี่ยม แล้วคำนวณพื้นที่ของรูปสามเหลี่ยมดังกล่าว
*/
#include <stdio.h>

int main(void) {
  float a,b,c;
  scanf("%f", &a);
  scanf("%f", &b);
  printf("%f",0.5*a*b);
  
  return 0;
}