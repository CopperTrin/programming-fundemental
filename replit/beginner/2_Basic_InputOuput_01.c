/*
ให้นักศึกษาแก้ไขโปรแกรมตัวอย่างให้เป็นโปรแกรมรับ input เป็นตัวเลข 2 ตัว แล้วแสดงผลรวมของตัวเลขทั้งสอง
*/
#include <stdio.h>

int main(void) {
  int a,b;
  scanf("%d",&a);
  scanf("%d",&b);
  printf("%d", a+b);
  return 0;
}